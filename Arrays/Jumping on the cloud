// Problem: Jumping on the cloud
// Platform: Hackerrank
// Approach:
// Time Complexity:
// Space Complexity:

// Code 

class Solution{
  public int jumpingOnCloud(int []c){
      for(int i = 0; i < c.length;i++){
       if(i < c.length - 2){
           if(c[i + 2] != 1){
              jump++;
               i++;
           }
           else{
               jump++;
           }
       }
      else{
         jump++;
      }
      }
     return jump;
  }
}
